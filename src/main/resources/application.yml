spring:
  datasource:
    url: 'jdbc:postgresql://localhost:5432/lims'
    username: 'postgres'
    password: ''
  liquibase:
    enabled: true

  jpa:
    properties:
      hibernate:
        dialect: 'org.hibernate.dialect.PostgreSQLDialect'
    hibernate:
      ddl-auto: 'validate'
    show-sql: true


